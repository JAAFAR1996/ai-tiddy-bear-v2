class ConversationRepository(Protocol):from typing import Protocol, List, Optionalfrom uuid import UUIDfrom src.domain.entities.conversation import Conversationclass ConversationRepository(Protocol):    async def save(self, conversation: Conversation) -> None: ...    async def get_by_id(self, conversation_id: UUID) -> Optional[Conversation]: ...    async def find_by_child_id(self, child_id: UUID) -> List[Conversation]: ...    async def delete(self, conversation_id: UUID) -> None: ...