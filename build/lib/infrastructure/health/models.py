"""from dataclasses import dataclassfrom datetime import datetimefrom enum import Enumfrom typing import Dict, List, Any"""Health check models and enums"""class HealthStatus(str, Enum):    """Health check status levels"""    HEALTHY = "healthy"    DEGRADED = "degraded"    UNHEALTHY = "unhealthy"    CRITICAL = "critical"@dataclassclass HealthCheckResult:    """Result of a health check"""    name: str    status: HealthStatus    message: str    details: Dict[str, Any]    duration_ms: float    timestamp: datetime@dataclassclass SystemHealth:    """Overall system health status"""    status: HealthStatus    uptime: float    checks: List[HealthCheckResult]    summary: Dict[str, Any]    timestamp: datetime