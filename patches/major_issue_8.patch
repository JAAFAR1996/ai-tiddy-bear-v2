--- a/src/presentation/routing.py
+++ b/src/presentation/routing.py
@@ -1,6 +1,6 @@
-from typing import Dict
+from typing import Dict, Optional
 import logging
 
 from fastapi import FastAPI
@@ -16,7 +16,7 @@
     # Set routers to None for graceful degradation
     esp32_router = parental_router = health_router = chatgpt_router = auth_router = None
 
-def setup_routing(app: FastAPI) -> None:
+def setup_routing(app: FastAPI) -> None:
     """Setup application routing with graceful degradation for missing modules"""
 
     # Include core endpoints only if available
